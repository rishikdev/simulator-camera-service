# A development-utility package for mocking hardware camera inputs in the iOS Simulator.
**Key Features** Network-agnostic frame streaming (via local network/sockets), @Observable state management, and seamless integration with existing DeviceCameraPreview interfaces via dependency injection.
**Tech Stack:** Swift 6, Network Framework, SwiftUI.